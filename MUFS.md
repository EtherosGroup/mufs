# Mount Unit File System — 技术规范

> **文档编号：** MUFS-SPEC-001  
> **版本：** 1.0  
> **状态：** Working Draft  
> **最后修订：** 2026-05-29  

---

## 目录

1. [引言](#1-引言)
2. [核心概念](#2-核心概念)
3. [底层文件系统选型](#3-底层文件系统选型)
4. [超级块扩展定义](#4-超级块扩展定义)
5. [路径解析规则](#5-路径解析规则)
6. [挂载点类型](#6-挂载点类型)
7. [虚拟挂载点 /devices](#7-虚拟挂载点-devices)
8. [系统启动流程](#8-系统启动流程)
9. [命令行接口](#9-命令行接口)
10. [数据安全与一致性保证](#10-数据安全与一致性保证)
11. [功能需求清单](#11-功能需求清单)
12. [非功能性需求](#12-非功能性需求)
13. [架构层次](#13-架构层次)
14. [附录A：魔数与常量定义](#附录a魔数与常量定义)
15. [附录B：术语表](#附录b术语表)

---

## 1. 引言

### 1.1 项目定位

**Mount Unit File System**（以下简称 MUFS）是一种以**挂载点**（Mount Point）为一级组织原语的内核态文件系统框架。与传统文件系统以目录树为中心的模型不同，MUFS 将挂载点提升为存储资源访问的唯一入口，重新定义了文件系统的命名空间结构。

### 1.2 设计目标

| 目标 | 说明 |
|------|------|
| 挂载点作为一等公民 | 所有存储资源必须通过挂载点访问，根目录本身不可存储 |
| 类型化挂载 | 通过 `DMP` / `SMP` / `VMP` 三种类型精确区分物理设备、系统路径与虚拟资源 |
| 分区自描述 | 挂载元数据持久化在分区自身的超级块中，分区与配置不可分割 |
| 路径动态化 | 系统路径不硬编码，通过变量机制在初始化阶段动态映射 |
| 内核态一体化 | 自内核初始化阶段启动，无需用户态介入或外部配置文件 |

### 1.3 与传统模型的关键差异

| 维度 | 传统文件系统 | MUFS |
|------|------------|------|
| 组织原语 | 目录树（Directory Hierarchy） | 挂载点（Mount Point） |
| 根目录 | 可容纳文件和子目录 | 设备自身标识符，不可写 |
| 挂载配置 | 外部配置文件（如 `/etc/fstab`） | 分区超级块内嵌元数据 |
| 系统路径 | 编译期或安装期硬编码（`/tmp`, `/home`） | 运行时动态映射（`%temp%`, `%home%`） |
| 设备枚举 | 静态设备节点 + 守护进程 | `/devices` 内核态动态生成 |
| 启动依赖 | initramfs → pivot_root → fstab | 内核模块直接初始化 |

---

## 2. 核心概念

### 2.1 根目录重定义

`/` 在 MUFS 语义下表示**当前设备自身**，不承载任何文件或目录。

**硬约束：**
- `/` 不可作为容器 —— 拒绝任何文件/目录创建操作
- 合法路径深度 ≥ 2，即必须包含至少一个挂载点
- `/` 下的第一级条目必须全部为挂载点

### 2.2 挂载点模型

每个挂载点是操作系统与应用程序访问存储资源的唯一端口，与一个物理分区或虚拟资源一一对应。

**挂载点元组：**

| 属性 | 类型 | 说明 |
|------|------|------|
| 挂载名 | `char[48]` | 在 `/` 下暴露的命名 |
| 挂载类型 | 枚举 | `DMP` \| `SMP` \| `VMP` |
| 标志位 | 位图 | 只读、隐藏、禁止卸载、系统关键 |
| 自动挂载 | `u8` | `0x01`=启动时自动挂载，`0x00`=手动挂载 |
| 变量标签 | `char[16]` | 可选，关联的系统变量名 |

---

## 3. 底层文件系统选型

### 3.1 主文件系统：ext4

MUFS 采用 **ext4** 作为底层持久化文件系统。选型依据如下：

| 评估维度 | ext4 表现 | 结论 |
|---------|----------|------|
| 超级块预留空间 | 约 500 字节未使用区域（`s_reserved[190]`） | 可容纳完整 MUFS 扩展（160 字节） |
| 规范开放性 | 完全公开，Linux 内核源码可交叉参考 | 通过 |
| 超级块备份 | 块组 1, 3, 5, 7, 9, 25, 27, 49... 多处镜像 | 元数据天然冗余 |
| 写入模式 | ordered 模式：数据先于元数据落盘 | 崩溃一致性有保证 |
| 久经考验 | 生产环境验证超过 15 年 | 社区信任度过关 |

### 3.2 其他候选文件系统评估

| 文件系统 | 结论 | 理由 |
|---------|------|------|
| XFS | 不可用 | 超级块结构紧促，无可用预留空间 |
| Btrfs | 不推荐 | 预留区域定义不明确，稳定性尚在演进 |
| NTFS | 不可用 | 规范不完全公开，VBR 空间极小且可能被宿主 OS 覆写 |
| FAT32 / exFAT | 不推荐 | 超级块空间紧促，仅建议用于跨平台数据交换分区 |

### 3.3 跨平台数据分区策略

需跨 OS 共享数据的分区应使用 **exFAT** 格式，且不作为 MUFS 系统分区：
- 不写入 MUFS 超级块扩展
- 作为普通 `DMP` 挂载，挂载名通过 `mount` 命令参数指定

---

## 4. 超级块扩展定义

### 4.1 设计原理

MUFS 将挂载元数据直接写入分区超级块的预留区域，从而实现**分区级别的自描述**：
- 分区移动或迁移时，挂载配置自动跟随，无需外部文件同步
- 分区与其挂载属性构成不可分割的原子整体
- 系统初始化时仅需扫描块设备即可重建完整挂载拓扑

### 4.2 ext4 超级块存储布局

ext4 超级块位于分区偏移 `0x400`（1024）字节处，总长 1024 字节。

```
偏移         区域                用途
0x000–0x18F  标准字段             ext4 核心元数据
0x190–0x1FF  日志字段             ext4 日志配置
0x200–0x3FF  预留区域             MUFS 扩展写入于此
```

内核源码对应结构片段：

```c
struct ext4_super_block {
    /* ... 标准字段 ... */
    __le32  s_journal_inum;
    /* ... 更多字段 ... */
    __le32  s_reserved[190];   // 偏移 ≈ 0x200，MUFS 占用
};
```

### 4.3 MUFS 扩展结构

#### 4.3.1 扩展头（56 字节，`offset = 0x200`）

| 偏移 | 字段 | 大小 | 类型 | 说明 |
|------|------|------|------|------|
| `0x00` | `mu_magic` | 4 B | `u32` | 魔数 `0x4D554653`（ASCII: `"MUFS"`） |
| `0x04` | `mu_version` | 2 B | `u16` | 扩展版本号，当前 `0x0001` |
| `0x06` | `mu_flags` | 2 B | `u16` | 挂载标志位（见 §4.4） |
| `0x08` | `mu_name` | 48 B | `u8[48]` | 挂载名，UTF-8，最大 47 字符，`\0` 终止 |

#### 4.3.2 扩展体（104 字节，紧随扩展头）

| 偏移 | 字段 | 大小 | 类型 | 说明 |
|------|------|------|------|------|
| `0x00` | `mu_type` | 1 B | `u8` | 挂载点类型：`0x01`=DMP, `0x02`=SMP, `0x03`=VMP |
| `0x01` | `mu_perm` | 1 B | `u8` | 权限位（见 §4.5） |
| `0x02` | `mu_auto_mount` | 1 B | `u8` | 自动挂载标志：`0x01`=启动时自动挂载, `0x00`=手动挂载 |
| `0x03` | `mu_var_tag` | 16 B | `u8[16]` | 变量标签，如 `"temp"`，`\0` 终止 |
| `0x13` | `mu_var_target` | 16 B | `u8[16]` | 变量子路径，如 `"temp/"`，`\0` 终止 |
| `0x23` | `mu_checksum` | 4 B | `u32` | 扩展体 CRC32 校验和 |
| `0x27` | `mu_reserved` | 65 B | `u8[65]` | 预留，供未来版本使用 |

> **扩展总大小：** 56 + 104 = **160 字节**（≪ ext4 预留的 ~500 字节）

### 4.4 标志位定义 (`mu_flags`)

| Bit | 常量 | 值 | 说明 |
|-----|------|-----|------|
| 0 | `MUFS_FLAG_READONLY` | `0x0001` | 挂载点为只读 |
| 1 | `MUFS_FLAG_HIDDEN` | `0x0002` | 不枚举到 `/devices` |
| 2 | `MUFS_FLAG_NO_UNMOUNT` | `0x0004` | 禁止卸载 |
| 3 | `MUFS_FLAG_SYSTEM_CRITICAL` | `0x0008` | 系统关键分区 |
| 4–15 | — | — | 保留 |

### 4.5 权限位定义 (`mu_perm`)

| Bit | 说明 |
|-----|------|
| 0 | 只读挂载 |
| 1 | 禁止用户卸载 |
| 2 | 系统关键（卸载前须二次确认） |
| 3–7 | 保留 |

### 4.6 超级块备份同步

ext4 在多个块组中维护超级块镜像（块组 1, 3, 5, 7, 9, 25, 27, 49...）。MUFS 写入流程要求：

1. 写入主超级块 → 立即 `fsync()`
2. 遍历所有备份超级块 → 逐一写入并 `fsync()`

该机制确保主超级块损坏时，可从任一备份完整恢复 MUFS 元数据。

### 4.7 向后兼容性

- MUFS 扩展完全驻留 ext4 预留区域，不触碰任何标准字段
- 无 MUFS 模块的系统挂载此类分区时，该区域被静默忽略
- 缺失 MUFS 魔数（`0x4D554653`）的分区按传统方式处理

---

## 5. 路径解析规则

### 5.1 路径语法

```
/<mount-point>/<file-path>...
```

### 5.2 解析流程

```
输入路径
    │
    ▼
┌─────────────────────────┐
│ 1. 提取首级组件          │ → 挂载点名
├─────────────────────────┤
│ 2. 查挂载点映射表        │ → 哈希表 O(1)
├─────────────────────────┤
│ 3. 校验类型与权限        │ → 检查 flags / perm
├─────────────────────────┤
│ 4. 委托底层 FS 处理      │ → 非 VMP 路径
│    MUFS 自行解析         │ → VMP 路径
└─────────────────────────┘
```

### 5.3 路径示例

```
/system/tenon/config/
```

| 组件 | 解析 |
|------|------|
| `/` | 当前设备 |
| `system/` | 名为 `"system"` 的 SMP 挂载点 |
| `tenon/config/` | ext4 文件系统上的普通目录树 |

### 5.4 安全边界

- `..` 上升至挂载点边界时被阻断（路径穿越防护）
- `/` 拒绝任何写入操作
- VMP 路径完全由内核模块动态合成，不接受外部输入

---

## 6. 挂载点类型

### 6.1 Device Mount Point (DMP)

映射到物理块设备的挂载点。

**特征：**
- 由用户通过 `mount` 命令创建
- 必须关联一个块设备路径
- 支持标准的读/写语义
- 超级块中可预置挂载名

```bash
mount DMP /devices/sda1 data    # → /data/...
```

### 6.2 System Mount Point (SMP)

由系统在安装或初始化阶段创建的关键挂载点。

**特征：**
- 仅安装阶段可创建，普通用户无权创建/删除
- 默认置位 `MUFS_FLAG_SYSTEM_CRITICAL` 和 `MUFS_FLAG_NO_UNMOUNT`
- 通常关联一个或多个系统变量标签

```bash
mount SMP /devices/sda2 system    # → /system/...
                                  #     %temp% → /system/temp/
```

### 6.3 Virtual Mount Point (VMP)

不对应物理存储、内容由内核动态生成的虚拟挂载点。

**特征：**
- 不关联块设备
- 内容由 MUFS 内核模块运行时合成
- 默认只读
- `/devices` 为系统强制创建的 VMP

**预定义 VMP：**

| 名称 | 说明 | 创建方式 |
|------|------|---------|
| `/devices` | 已连接设备清单 | 系统强制创建 |
| `/network` | 网络资源视图（可选） | 网络服务触发 |

---

## 7. 虚拟挂载点 /devices

### 7.1 生命周期

- `/devices` 由 MUFS 内核模块在初始化阶段自动创建
- 声明为系统强制 VMP，不可删除、重命名或覆盖
- 生命周期与内核运行周期绑定

### 7.2 动态内容

`/devices` 实时反映当前连接的全部存储设备：

```
/devices/
  sda/           ── 物理磁盘
  sda1/          ── 分区 1
  sda2/          ── 分区 2
  nvme0n1/       ── NVMe 磁盘
  nvme0n1p1/     ── NVMe 分区
  sr0/           ── 光驱
```

每个条目暴露的元数据包括：设备路径、分区 UUID、文件系统类型、挂载状态及挂载点名。

### 7.3 行为约束

- 只读 —— 拒绝所有写入操作
- 实时 —— 设备热插拔事件立即反映
- 隐匿 —— `MUFS_FLAG_HIDDEN` 置位的分区不列出
- 锁定 —— 对 `/devices` 自身的卸载操作被内核拒绝

---

## 8. 系统启动流程

### 8.1 启动链

```
固件 (UEFI)
    │
    ▼
引导程序 (Bootloader)
    │
    ▼
内核 + MUFS 模块加载至内存
    │
    ▼
内核基础初始化
    │
    ▼
┌──────────────────────────────────┐
│      MUFS 模块初始化              │
│  1. 枚举所有块设备                │
│  2. 读取各分区超级块偏移 0x200    │
│  3. 匹配魔数 0x4D554653           │
│  4. CRC32 校验                    │
│  5. 提取挂载元数据                │
│  6. 检查 mu_auto_mount 标志       │
│  7. 自动挂载 auto_mount=1 的分区   │
│  8. 构建挂载点映射表（哈希表）    │
│  9. 创建 /devices VMP             │
│ 10. 建立系统变量映射表            │
└──────────────────────────────────┘
    │
    ▼
挂载点就绪，用户空间可见
    │
    ▼
init 进程启动
```

### 8.2 引导程序集成（以 GRUB 为例）

引导程序阶段完全独立于 MUFS，仅需能够定位并加载内核镜像。

引导程序定位内核的典型步骤：
1. 解析 GPT 分区表
2. 从 ESP（FAT32）加载引导程序本体
3. 读取引导配置文件
4. 通过分区 UUID 定位内核所在分区
5. 加载内核镜像及初始 RAM 磁盘

**引导配置示例：**
```bash
menuentry 'MUFS OS' {
    search --no-floppy --fs-uuid --set=root 1234-ABCD
    linux /boot/vmlinuz root=UUID=5678-EFGH ro quiet
    initrd /boot/initrd.img
}
```

### 8.3 与传统启动模型的对比

| 阶段 | 传统模型 | MUFS 模型 |
|------|---------|----------|
| 引导程序 | Bootloader | Bootloader（不变） |
| 内核定位 | 按 UUID 加载 | 完全一致 |
| 根文件系统初始化 | initramfs → pivot_root → 挂载 fstab | MUFS 模块直接构建 |
| 挂载配置来源 | 外部文件 | 分区超级块扩展 |
| 系统路径确定 | 编译期/安装期硬编码 | 启动时动态映射 |

---

## 9. 命令行接口

### 9.1 mount 命令

```
mount <operation> <device-path> [mount-name]
```

| 操作类型 | 行为 |
|---------|------|
| `DMP` | 创建设备挂载点 |
| `SMP` | 创建系统挂载点（权限受限） |
| `VMP` | 创建虚拟挂载点（内核严格控制） |
| `unmount` | 卸载挂载点 |

### 9.2 命令示例

```bash
# DMP — 使用设备名作为默认挂载名
mount DMP /devices/sda1          # → /sda1/

# DMP — 指定挂载名，持久化至超级块
mount DMP /devices/sda1 data     # → /data/, mu_name = "data"

# SMP — 仅安装阶段可调用
mount SMP /devices/sda2 system   # → /system/, mu_type = 0x02

# 卸载（元数据保留在超级块中）
mount unmount data               # → /data/ 消失

# 非法操作 — VMP 由内核控制
mount VMP devices                # → 错误: /devices 为强制 VMP
```

### 9.3 mufsctl — 管理工具

```bash
mufsctl list                # 列出所有挂载点
mufsctl info /system        # 查看挂载点详情
mufsctl vars                # 查看系统变量映射
mufsctl check /dev/sda1     # 验证超级块 MUFS 元数据
mufsctl repair /dev/sda1    # 从备份恢复 MUFS 元数据
```

### 9.4 mufs.mkfs — 安装格式化工具

```bash
mufs.mkfs \
    --type SMP \
    --name system \
    --var temp:temp/ \
    --var home:users/ \
    --var logs:logs/ \
    /dev/sda2
```

**内部流程：**
1. 调用 `mkfs.ext4` 创建 ext4 文件系统
2. 打开块设备 → seek 至超级块 `offset 0x200`
3. 写入 MUFS 扩展头 + 扩展体
4. 计算并写入 CRC32
5. `fsync()` 强制刷盘
6. 同步写入所有备份超级块

---

## 10. 数据安全与一致性保证

### 10.1 ext4 数据安全模型

ext4 默认运行于 **ordered** 写入模式：
- 文件数据在元数据更新前先写入磁盘
- 断电恢复时，已提交元数据对应的数据保证已落盘
- 页缓存中尚未开始写入的数据可能丢失（所有基于页缓存的现代文件系统的共同特性）

### 10.2 MUFS 元数据完整性保证

MUFS 超级块扩展被视为关键元数据，强制执行以下安全措施：

**同步写入链：**  
写主超级块 → `fsync()` → 遍历备份超级块 → 逐一写入并 `fsync()`

**CRC32 哨兵：**  
每次读取扩展体时重新计算 CRC32 与存储值比对；校验失败自动回退至备份超级块恢复。

**备份恢复：**
- ext4 在块组 1, 3, 5, 7, 9... 处维护超级块镜像
- 写入主超级块时同步刷新所有镜像
- 自动恢复失败时可手动执行 `mufsctl repair`

### 10.3 数据安全责任边界

| 数据类别 | 安全保障方 | 建议 |
|---------|-----------|------|
| MUFS 超级块元数据 | MUFS 模块（同步写入 + CRC32） | 自动保证 |
| 系统配置文件 | 应用程序（`fsync`） | 系统服务应在写入关键配置后调用 `fsync` |
| 用户数据 | 应用程序 | 关键数据写入后显式调用 `fsync` |
| 数据库文件 | 数据库引擎 | 引擎已内置事务安全机制 |

### 10.4 挂载参数建议

```bash
# 系统分区 — 以安全为优先
mount -o data=ordered,commit=5 /dev/sda2 /

# 数据分区 — 以吞吐为优先
mount -o data=ordered,commit=30 /dev/sda3 /data
```

---

## 11. 功能需求清单

### 11.1 核心功能

| ID | 功能 | 优先级 |
|----|------|--------|
| F1 | 挂载点生命周期管理（创建、查询、卸载） | P0 |
| F2 | 路径解析与标准化 | P0 |
| F3 | DMP 完整实现 | P0 |
| F4 | SMP 完整实现（含安装阶段工具） | P0 |
| F5 | VMP 框架与 `/devices` 强制创建 | P0 |
| F6 | 系统变量动态解析引擎 | P0 |
| F7 | ext4 超级块扩展读写 | P0 |
| F8 | 超级块备份同步机制 | P0 |
| F9 | 魔数检测与分区自动发现 | P0 |
| F10 | 设备热插拔事件监听与 `/devices` 实时更新 | P1 |
| F11 | CRC32 校验与自动备份恢复 | P0 |
| F12 | `mufsctl` 用户态管理工具 | P1 |
| F13 | `mufs.mkfs` 安装格式化工具 | P1 |

### 11.2 安全约束

| ID | 约束 | 级别 |
|----|------|------|
| S1 | 根目录 `/` 不可写，不可创建节点 | 强制 |
| S2 | SMP 创建/卸载受权限与超级块标志双重保护 | 强制 |
| S3 | `/devices` 不可删除、卸载或覆盖 | 强制 |
| S4 | `..` 不可穿越挂载点边界（路径穿越防护） | 强制 |
| S5 | 超级块扩展体 CRC32 校验 | 强制 |
| S6 | 无 MUFS 魔数的分区按传统方式降级处理 | 强制 |
| S7 | 系统关键分区卸载前须二次确认 | 强制 |
| S8 | MUFS 元数据写入必须同步刷盘 | 强制 |

---

## 12. 非功能性需求

### 12.1 性能指标

| 指标 | 目标值 | 实现策略 |
|------|--------|---------|
| 挂载点查找 | O(1) | 哈希表，键为挂载名 |
| 路径解析延迟 | < 100 µs | 内核态直接路径，零上下文切换 |
| 超级块扫描 | 仅启动 + 热插拔 | 运行时零开销 |
| 变量解析 | 命中 O(1) | 首次解析后缓存 |
| 元数据写入延迟 | < 10 ms | 同步刷盘，含备份同步 |

### 12.2 可靠性

- 超级块扩展损坏：自动从备份恢复，成功率 > 99.9%
- MUFS 模块内部错误：降级为只读模式，禁止 panic
- 外部输入（挂载名、变量标签）：强制校验长度上限与字符集白名单
- 系统关键分区卸载：强制二次确认交互

### 12.3 可维护性

**内核模块分层：**

| 模块 | 职责 |
|------|------|
| `mufs_superblock` | 超级块读写、备份同步与 CRC32 |
| `mufs_mount` | 挂载点生命周期管理 |
| `mufs_path` | 路径解析与规范化 |
| `mufs_vmp` | 虚拟挂载点内容生成 |
| `mufs_vars` | 系统变量映射与缓存 |

- 用户态工具通过 sysfs 接口与内核模块通信
- 超级块扩展结构带版本号，向前兼容

### 12.4 兼容性

- 无 MUFS 模块的系统挂载 MUFS 分区：超级块扩展被静默忽略
- 无 MUFS 魔数的分区在 MUFS 系统中按传统挂载方式处理
- 完整支持 ext4 标准功能（日志、扩展属性、ACL 等）

---

## 13. 架构层次

### 13.1 系统架构

```
                         用户空间应用程序
                  MUFS 统一命名空间视图
             /system    /data    /devices    ...

                     ── 系统调用接口 ──

                        MUFS 内核模块
            ┌───────────┼───────────┐
        挂载点管理   路径解析引擎   系统变量引擎
        超级块读写   VMP 生成器    热插拔监听
            └───────────┼───────────┘

                    虚拟文件系统层 (VFS)

                  实际文件系统驱动
            ext4      exFAT      ...

                     块设备抽象层
```

### 13.2 与传统挂载体系的对比

| 维度 | 传统模型 | MUFS 模型 |
|------|---------|----------|
| 挂载点约束 | 可置于任意目录 | 仅 `/` 下第一级 |
| 配置存储 | 外部文本文件 | 分区超级块二进制扩展 |
| 根目录语义 | 初始根文件系统，可写 | 设备标识符，不可写 |
| 分区自描述 | 否 | 是 |
| 系统路径 | 硬编码字符串 | 动态变量映射 |
| 设备视图 | 静态设备节点 + 用户态守护 | `/devices` 内核态动态 |
| 挂载点类型 | 无类型区分 | DMP / SMP / VMP 三态 |
| 启动依赖 | initramfs + fstab 多阶段 | 内核模块一体化初始化 |
| 配置粒度 | 系统级（单文件） | 分区级（分布式） |

---

## 附录A：魔数与常量定义

```c
// 魔数与版本
#define MUFS_MAGIC          0x4D554653  /* "MUFS" */
#define MUFS_VERSION        0x0001

// 布局常量
#define MUFS_EXT_OFFSET     0x200       /* ext4 超级块内偏移 */
#define MUFS_HEAD_SIZE      56          /* 扩展头（字节） */
#define MUFS_BODY_SIZE      104         /* 扩展体（字节） */
#define MUFS_EXT_TOTAL_SIZE 160         /* 扩展总大小（字节） */

// 字符串限制
#define MUFS_NAME_MAX       48          /* 挂载名最大长度 */
#define MUFS_VAR_TAG_MAX    16          /* 变量标签最大长度 */

// 挂载点类型枚举
#define MUFS_TYPE_DMP       0x01        /* Device Mount Point */
#define MUFS_TYPE_SMP       0x02        /* System Mount Point */
#define MUFS_TYPE_VMP       0x03        /* Virtual Mount Point */

// 标志位
#define MUFS_FLAG_READONLY          0x0001
#define MUFS_FLAG_HIDDEN            0x0002
#define MUFS_FLAG_NO_UNMOUNT        0x0004
#define MUFS_FLAG_SYSTEM_CRITICAL   0x0008
```

---

## 附录B：术语表

| 术语 | 全称 | 定义 |
|------|------|------|
| **MUFS** | Mount Unit File System | 以挂载点为一级组织原语的文件系统框架 |
| **DMP** | Device Mount Point | 设备挂载点，对应物理块设备分区 |
| **SMP** | System Mount Point | 系统挂载点，由安装程序创建，权限高于 DMP |
| **VMP** | Virtual Mount Point | 虚拟挂载点，内容由内核动态生成，无物理存储 |
| **超级块** | Superblock | 文件系统全局元数据块，记录分区参数 |
| **魔数** | Magic Number | MUFS 分区标识常量 `0x4D554653` |
| **系统变量** | System Variable | `%<tag>%` 语法，动态映射至物理路径 |
| **挂载点** | Mount Point | MUFS 中用户访问存储资源的唯一入口 |

---

*文档结束 · MUFS-SPEC-001 v1.0*
